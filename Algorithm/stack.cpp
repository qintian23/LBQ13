#include <stdio.h>

typedef int T;

// 顺序栈定义
typedef struct 
{
    T* base; // 栈底指针
    T* top; // 栈顶指针
    int size; // 当前可用最大容量
}sqStack;

// 栈的初始化
#define STACK


int main(int argc, char** argcv)
{
    return 0;
}